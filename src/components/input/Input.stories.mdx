import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import Input from "./";

<Meta title="Components/Input" component={Input} />

export const Template = (args) => <Input {...args} />;

export const Decorator = (S) => (
  <div
    style={{
      width:500,
      display: "inline-flex",
      justifyContent: "flex-start",
      alignItems: "center",
      flexWrap: "wrap",
      gap: "1rem",
    }}
  >
    <S />
  </div>
);

# Inputs

This is a sample

## Properties

<ArgsTable of={Input} />





## Toggle

<Canvas>
  <Story name="Toggle" decorators={[Decorator]}>
    <Input type="toggle" />
  </Story>
</Canvas>

## Pin

<Canvas>
  <Story name="Pin" decorators={[Decorator]}>
    <Input type="pin" />
  </Story>
</Canvas>

## Textbox

<Canvas>
  <Story name="Textbox" decorators={[Decorator]}>
    <Input type="textbox"  />
  </Story>
</Canvas>

## Rich textbox

<Canvas>
  <Story name="Rich Textbox" decorators={[Decorator]}>
    <Input type="richtextbox" />
  </Story>
</Canvas>

