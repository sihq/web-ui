import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";


import Example from './controlled';

<Meta title="Components/Examples" component={Example} />

export const Decorator = (S) => (
  <div
    style={{
      width:500,
      display: "inline-flex",
      justifyContent: "flex-start",
      alignItems: "center",
      flexWrap: "wrap",
      gap: "1rem",
    }}
  >
    <S />
  </div>
);

# Example

This is a sample


<Canvas>
  <Story name="example" decorators={[Decorator]}>
    <Example />
  </Story>
</Canvas>
